## General

This repository is the `laravel-skir/typescript-client` fork of `gepheum/skir-typescript-client`.

Keep upstream compatibility unless a change is explicitly for the Laravel Skir package ecosystem.

## Git

Commit as `Maxim Kerstens <maxim.kerstens@gmail.com>`.

Do not mention Codex, ChatGPT, or AI tooling in commits, pull requests, issues, comments, or package documentation.

## Verification

Before committing source changes, run:

```bash
npm run format:check
npm run lint
npm run build
npm test
```
